from nitrostack import injectable


@injectable(deps=[])
class CLASS_NAME:
    """Injectable service. Register it in a module's ``providers`` list."""

    def __init__(self):
        # TODO: add constructor dependencies via @injectable(deps=[...])
        pass
